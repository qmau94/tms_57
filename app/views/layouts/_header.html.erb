<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to root_path do %>
        <%= image_tag "framgia2.png", class: "header-img" %>
      <% end %>  
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><%= link_to t("navbar.user"), "#" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t("navbar.account") %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("navbar.profile"), "#" %></li>
              <li><%= link_to t("navbar.setting"), "#" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t("navbar.logout"), logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t("static_pages.login"), login_path %>
          </li>
          <li>
            <%= link_to t("static_pages.about"), about_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
